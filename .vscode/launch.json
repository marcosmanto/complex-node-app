{
  "version": "0.2.0",
  "configurations": [

    {
      "name": "Debug current file",
      "program": "${workspaceFolder}/${file}",
      "request": "launch",
      "skipFiles": [
        "<node_internals>/**"
      ]
    }

  ]
}